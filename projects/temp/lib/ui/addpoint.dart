import 'package:flutter/material.dart';
import 'package:get/get.dart';

class addpoint extends StatelessWidget {
  const addpoint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("add_point".tr),
    );
  }
}
